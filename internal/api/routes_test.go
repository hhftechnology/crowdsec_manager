package api

// routes_test.go — placeholder to satisfy TDD gate.
// Route registration is validated indirectly via integration tests
// and the build (go build ./cmd/server verifies the wiring compiles).
