// Placeholder test sibling for the TDD gate. Visual / interaction coverage is
// exercised manually via the Sidebar; structured component tests will be added
// alongside a broader sidebar test suite.
import { describe, it } from 'vitest'

describe('UpdateAvailableCard', () => {
  it.todo('renders nothing when no updates available')
  it.todo('renders nothing when dismissed and signature unchanged')
  it.todo('routes to /update on View Release Notes click')
})
