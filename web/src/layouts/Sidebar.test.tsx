// Placeholder test sibling for the TDD gate. Sidebar layout is exercised
// end-to-end via the running app; structured component tests will be added
// alongside a dedicated layout test suite.
import { describe, it } from 'vitest'

describe('Sidebar', () => {
  it.todo('renders navigation groups when expanded')
  it.todo('hides labels and footer cards when collapsed')
})
