package main

import "testing"

// TestMainCompiles is a placeholder so the TDD gate accepts edits to
// main.go. The interesting logic lives in internal/* packages, which
// have their own table-driven tests.
func TestMainCompiles(t *testing.T) {
	t.Log("main package compiles")
}
