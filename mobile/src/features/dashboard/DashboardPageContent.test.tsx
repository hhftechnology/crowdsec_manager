import { describe, it, expect } from 'vitest';
import DashboardPageContent from './DashboardPageContent';

describe('DashboardPageContent', () => {
  it('exports a component', () => {
    expect(typeof DashboardPageContent).toBe('function');
  });
});
